/** Standard weight of water: 8.34 lbs per gallon at 60 degrees F. */
export const WATER_LBS_PER_GALLON = 8.34

/** Approximate weight of one cubic yard of concrete in pounds. */
export const POUNDS_PER_CUBIC_YARD = 3800

/** Convergence threshold for iterative ratio-balancing algorithms. */
export const RATIO_CONVERGENCE_TOLERANCE = 0.001

/** Maximum passes for iterative ratio-balancing before giving up. */
export const MAX_BALANCING_ITERATIONS = 10

/** Additions below this threshold (in lbs) are treated as negligible. */
export const NEGLIGIBLE_ADDITION_THRESHOLD = 0.5
